import { IPokemon } from 'interfaces';

interface IProps {
  currentPage: IPokemon[];
  nextPage: IPokemon[];
}

const PokemonGrid = ({ currentPage, nextPage }: IProps) => {
  return <></>;
};

export default PokemonGrid;
